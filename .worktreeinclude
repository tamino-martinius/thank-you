# Gitignored files Conductor copies from the repo root into every new workspace.
# Uses .gitignore syntax. (Conductor's default is already `.env*`; this makes it
# explicit and adds the optional import folder.)

# Optional GH_PAT for `npm run collect` / `npm run sync` (higher API rate limit).
.env
.env.*

# Optional local data exports, if you keep any.
data/import/
